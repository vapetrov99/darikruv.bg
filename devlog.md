# Development Log – DariKruv

## 10.03.2026

### Подготовка на средата за разработка

Днес беше създадена основната структура на проекта и средата за разработка.

- Инсталиран Docker Desktop
- Инсталиран Git
- Създаден Git repository
- Създаден .gitignore файл
- Създадена структура на проекта

Структура:

darikruv/
- docker/
  - nginx/
  - php/
- src/
- docker-compose.yml
- .gitignore

### Docker конфигурация

Създадени са следните файлове:

- docker-compose.yml
- docker/php/Dockerfile
- docker/nginx/default.conf

Docker средата съдържа:

- Nginx (web server)
- PHP 8.2
- MySQL 8

### Първоначален backend тест

Създаден е тестов файл:

src/index.php

който потвърждава, че PHP работи правилно.

### Version Control

Инициализиран е Git repository и е направен първи commit.


## 12.03.2026

### Проектиране на базата данни

Беше разработена началната схема на базата данни за системата DariKruv.

Създадени бяха основните таблици:
- users
- donors
- blood_requests

Беше дефинирана връзка между потребителите и донорите, както и структура за публикуване на заявки за кръв.

### Конфигурация на backend връзката към базата

Създаден беше конфигурационен файл за PHP връзка към MySQL чрез PDO.

## 16.03.2026

### Свързване на backend-а с базата данни

Беше импортирана началната SQL схема в MySQL контейнера.
Бяха създадени тестови PHP файлове за проверка на връзката към базата данни, запис на тестов потребител и извличане на данни от таблицата users.

Чрез тези тестове беше потвърдено, че PHP приложението комуникира успешно с MySQL чрез PDO.

CRUD тестовете работят

### Начало на API слоя

Създаден беше начален API entry point за backend приложението.
Реализиран беше първи endpoint за извличане на потребители от базата данни и връщането им в JSON формат.

Така проектът премина от тестови PHP файлове към начална REST API архитектура.

Проектът е успешно качен в GitHub! 

## 17.03.2026

### Разработка на REST API слой

Днес беше реализирана основната логика на backend API слоя на системата. Създаден беше централен входен файл (`api/index.php`), който обработва HTTP заявки и маршрутизира различните endpoints в зависимост от HTTP метода и подадения route параметър.

---

### Реализиране на регистрация на потребители

Беше разработен POST endpoint за регистрация на потребители (`route=register`).

Реализирани функционалности:

- Приемане на JSON данни от клиента
- Валидация на задължителните полета (име, имейл, парола)
- Проверка на валидността на имейл адрес
- Проверка за минимална дължина на паролата
- Проверка за съществуващ потребител със същия имейл
- Хеширане на паролата чрез `password_hash()`
- Запис на нов потребител в базата данни

При успешно изпълнение системата връща JSON отговор със статус и данни за създадения потребител.

---

### Реализиране на вход в системата (Login)

Беше разработен POST endpoint за автентикация (`route=login`).

Реализирани функционалности:

- Приемане на JSON данни (email и password)
- Валидация на входните данни
- Извличане на потребител от базата данни
- Проверка на паролата чрез `password_verify()`
- Връщане на информация за потребителя без чувствителни данни (парола)

При неуспешен вход системата връща HTTP статус код 401 (Unauthorized).

---

### Работа с HTTP статус кодове и JSON отговори

Имплементирана беше обработка на различни HTTP статус кодове:

- 200 – успешна заявка
- 201 – успешно създаване на ресурс
- 400 – невалидни входни данни
- 401 – неуспешна автентикация
- 409 – конфликт (съществуващ потребител)
- 500 – сървърна грешка

Всички отговори се връщат в JSON формат, което позволява лесна интеграция с frontend частта на системата.

---

### Тестване на API функционалностите

Беше проведено тестване на API endpoints чрез PowerShell (`Invoke-RestMethod`), включително:

- Успешна регистрация на потребител
- Валидация при липсващи или невалидни данни
- Проверка за дублиран имейл
- Успешен и неуспешен вход в системата

Резултатите показват, че API-то функционира коректно и обработва всички основни сценарии.

---

### Отстраняване на проблеми (Debugging)

Беше идентифициран проблем, при който след успешен отговор се връщаше допълнително съобщение „Route not found“. Причината беше липсващо прекратяване на изпълнението (`exit`) след изпращане на JSON отговор.

След добавяне на `exit` във всички endpoints проблемът беше отстранен и системата започна да връща коректен единичен отговор.

---

### Обобщение

С реализираните функционалности беше изградена основата на backend системата, включително:

- REST API архитектура
- Регистрация и вход на потребители
- Валидация и защита на входните данни
- Работа с база данни чрез PDO

Тази функционалност представлява основа за бъдещото развитие на системата, включително управление на заявки за кръводаряване.

## 18.3.2026 – Разработка на модул за заявки за кръв и откликване

### Разширяване на модела за заявки за кръв

Беше анализирана структурата на заявките за кръв и беше взето решение да не се използва поле за степен на спешност (urgency). Причината е, че при подобен тип система почти всички заявки биха били маркирани като спешни, което би обезсмислило използването на такова поле.

Вместо това беше приет по-опростен и реалистичен модел, при който приоритетът на заявките се определя косвено чрез дата, описание и статус.

---

### Промяна в структурата на базата данни

Беше направена съществена промяна в таблицата `blood_requests`.

Премахнато поле:
- `required_donors_count`

Добавени полета:
- `required_units_count` – необходим брой единици/банки кръв
- `fulfilled_units_count` – брой вече осигурени единици

Тази промяна позволява по-реалистично проследяване на напредъка по всяка заявка и дава възможност за бъдеща логика като автоматично затваряне на заявка при достигане на нужния брой единици.

Базата данни беше презаредена с обновената структура.

---

### Реализиране на създаване на заявка за кръв

Беше разработен POST endpoint:

```text
route=create_request